{
  "name": "solofullstack",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "dev:build": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "dev:clean": "docker compose -f docker-compose.yml -f docker-compose.dev.yml down && docker compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "prod": "docker compose up --build",
    "down": "docker compose -f docker-compose.yml -f docker-compose.dev.yml down && docker compose down"
  }
}
